# Deprecated — use root `docker-compose.prod.yml` (Stage 8).
# Kept for backward reference; production overlay lives at repository root.

services:
  backend:
    environment:
      ENVIRONMENT: production
      DEBUG: "false"
